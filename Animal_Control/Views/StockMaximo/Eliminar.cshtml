@model Animal_Control.Models.AC_Stock_Maximo

@{
    ViewBag.Title = "Eliminar";
    Layout = "~/Views/Shared/_LayoutTablas.cshtml";
}

<section id="main-content" class=" ">
    <section class="wrapper main-wrapper" style=''>

        @using (Html.BeginForm())
            {
            @Html.AntiForgeryToken()

            <div>
                <h3>Eliminar Stock Maximo</h3>
                <h5>Quieres borrar este registro?</h5>
                <hr />
                <table cellspacing="0" width="100%">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Cantidad</th>
                        </tr>
                    </thead>

                    <tbody>
                        <tr>
                            <td>@Html.DisplayFor(modelItem => Model.SMA_ID) </td>
                            <td>@Html.DisplayFor(modelItem => Model.Cantidad)  </td>
                        </tr>
                    </tbody>

                </table>
                <br>
                <div class="form-actions no-color">
                    <input type="submit" value="Eliminar Stock Maximo" class="btn btn-default" />
                    <a href="@Url.Action("Index", "StockMaximo")" class="btn btn-default"> Cancelar </a>
                </div>

            </div>
        }
    </section>
</section>