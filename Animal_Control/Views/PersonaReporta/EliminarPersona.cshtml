@model Animal_Control.Models.AC_Persona_Reporta

@{
    ViewBag.Title = "Eliminar Persona";
    Layout = "~/Views/Shared/_LayoutTablas.cshtml";
}

<section id="main-content" class=" ">
    <section class="wrapper main-wrapper" style=''>

        @using (Html.BeginForm())
            {
            @Html.AntiForgeryToken()

            <div>
                <h3>Eliminar Articulo</h3>
                <h5>Quieres borrar este articulo?</h5>
                <hr />
                <table cellspacing="0" width="100%">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Cedula</th>
                            <th>Telefono</th>
                        </tr>
                    </thead>

                    <tbody>
                        <tr>
                            <td>@Html.DisplayFor(modelItem => Model.PR_ID) </td>
                            <td>@Html.DisplayFor(modelItem => Model.Nombre)  </td>
                            <td>@Html.DisplayFor(modelItem => Model.Cedula)  </td>
                            <td>@Html.DisplayFor(modelItem => Model.Telefono)  </td>
                        </tr>
                    </tbody>
                </table>
                <br>
                <br>
                <div class="form-actions no-color">
                    <input type="submit" value="Eliminar Articulo" class="btn btn-default" />
                    <a href="@Url.Action("Index", "PersonaReporta")" class="btn btn-default"> Cancelar </a>
                </div>
            </div>
        }
    </section>
</section>
